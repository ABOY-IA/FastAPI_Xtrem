FROM python:3.12-slim

WORKDIR /tests

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
RUN pip install alembic

COPY . .

# Commande par défaut : upgrade DB puis lancer les tests
CMD ["sh", "-c", "alembic upgrade head && pytest --maxfail=1 --disable-warnings -v"]
